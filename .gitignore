.DS_Store
node_modules/
.env
.env.local
.env.*.local
.next/
dist/
build/
*.log

# Email operations — contain PII; never commit
drafts/
logs/
inbox-cache/

# Operator dashboard — generated artifact (template + script are committed)
monitoring/dashboard.html

# Claude Code per-user / per-worktree state
.claude/settings.local.json
.claude/worktrees/
.vercel
.forks/
.env*.local

# Supabase CLI per-machine cache (project-ref, pooler URL, version metadata)
supabase/.temp/
